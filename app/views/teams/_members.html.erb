<div class="flex flex-wrap gap-6 items-center mt-10 mb-6">
  <h1 class="heading">Team Members</h1>
</div>

<div class="border-b border-slate-200 dark:border-slate-700">
  <%= form_with url: team_path(@team), method: :get, html: { novalidate: true }, data: { turbo_frame: 'teamMembers', turbo_action: 'advance', controller: 'search-form' } do |form| %>
    <%= form.label :query, 'Find a team member by name:', class: 'sr-only' %>

    <div class="form-group flex items-center gap-6 relative">
      <%= inline_svg_tag 'icons/search.svg', class: 'size-5 absolute left-3 text-slate-500' %>
      <%= form.search_field :query, placeholder: 'Team member name', data: { action: 'input->search-form#search' }, class: 'form-input pl-10' %>
      <%= form.submit 'Search', class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>

<div class="divide-y dark:divide-slate-700 dark:text-slate-700 mb-10">
  <%= turbo_frame_tag 'teamMembers' do %>
    <%= render 'member_list', members: members %>
  <% end %>
</div>
