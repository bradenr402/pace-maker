<% show_members = defined?(show_members) ? show_members : false %>

<div class="space-y-6">
  <% teams.each do |team| %>
      <%= render 'teams/shared/team', team: team, show_members: show_members %>
  <% end %>
</div>
