<%= turbo_stream.replace dom_id(@comment) do %>
  <%= render 'comments/comment', comment: @comment %>
<% end %>

<%= turbo_stream.replace 'new_comment_form' do %>
  <%= render 'new_form', comment: @comment.parent_run.comments.new %>
<% end %>
