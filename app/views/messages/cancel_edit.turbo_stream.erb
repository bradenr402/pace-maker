<%= turbo_stream.replace (@pin ? "pinned_#{dom_id(@message)}" : dom_id(@message)) do %>
  <%= render (@pin ? 'pinned_message' : @message), message: @message %>
<% end %>

<%= turbo_stream.replace 'new_message_form' do %>
  <%= render 'form', message: Message.new(topic: @topic) %>
<% end %>
