<% if @message.pinned? %>
  <%= turbo_stream.replace "pinned_#{dom_id(@message)}" do %>
    <%= render 'pinned_message', message: @message %>
  <% end %>
<% end %>

<%= turbo_stream.replace dom_id(@message) do %>
  <%= render @message %>
<% end %>
